// Implement a swap helper function that we will use in both BS and SS
function swap(arr, i, j) {}

//Build initial max heap by bubbling up on each element of input array
function buildMaxHeap(arr) {}

//heapSort works by having sorted data accumulate at end of array
function heapSort(arr) {}

module.exports = { heapSort, swap, buildMaxHeap };
